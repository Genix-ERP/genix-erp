import React from 'react';

// This component previously rendered fabricated "AI Sales Intelligence" narrative
// (hardcoded revenue forecasts, priority actions, and insight cards) with misleading
// "AI" / "Live Analysis" labels. None of that content was derived from the tenant's
// real data, so it has been removed. The component now renders nothing.
export default function CRMInsights() {
  return null;
}
